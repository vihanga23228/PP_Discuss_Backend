# Git Rules
- NEVER run `git commit` or `git push` automatically.
- Always ask for explicit user confirmation before staging, committing, or pushing code.
- Do NOT include any `Co-Authored-By` trailers in commit messages.